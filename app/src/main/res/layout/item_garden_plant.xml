<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <!-- Thumbnail -->
        <ImageView
            android:id="@+id/iv_garden_plant"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:scaleType="centerCrop"
            android:src="@drawable/placeholder_plant"
            android:background="@drawable/circle_image_bg" />

        <!-- Info -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginHorizontal="12dp">

            <TextView
                android:id="@+id/tv_garden_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Plant Name"
                android:textColor="@color/green_dark"
                android:textStyle="bold"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/tv_garden_scientific"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Scientific"
                android:textColor="@color/gray_medium"
                android:textStyle="italic"
                android:textSize="12sp"
                android:layout_marginTop="2dp" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="6dp">

                <TextView
                    android:id="@+id/tv_garden_watering"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="💧 Avg"
                    android:textColor="@color/green_medium"
                    android:textSize="11sp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text=" • "
                    android:textColor="@color/gray_medium"
                    android:textSize="11sp" />

                <TextView
                    android:id="@+id/tv_garden_cycle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Perennial"
                    android:textColor="@color/gray_medium"
                    android:textSize="11sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- Remove Button -->
        <ImageButton
            android:id="@+id/btn_remove"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_delete"
            android:background="@drawable/btn_remove_bg"
            app:tint="@color/error"
            android:contentDescription="Remove from garden" />
    </LinearLayout>

</androidx.cardview.widget.CardView>